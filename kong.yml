format_version: "2.1"

services:
  - name: my-api-server
    url: http://service_web_mitra_paylsa:3001
routes:
  - name: api-request
    service: my-api-server
    paths:
      - /api
plugins:
  - name: prometheus
â
